import { useState } from "react";
import { Button, Textarea } from "@/shared/ui";
export type InterviewResponseFormData={feedback:string};
export const InterviewResponseForm=({onSubmit,onCancel,isSubmitting,defaultValues}:{onSubmit:(data:InterviewResponseFormData)=>void;onCancel:()=>void;isSubmitting:boolean;defaultValues?:Partial<InterviewResponseFormData>})=>{const [feedback,setFeedback]=useState(defaultValues?.feedback??"");return <form onSubmit={e=>{e.preventDefault();onSubmit({feedback})}}><Textarea label="Feedback" rows={5} value={feedback} onChange={e=>setFeedback(e.target.value)}/><div className="mt-5 flex justify-end gap-3"><Button type="button" variant="secondary" onClick={onCancel}>Cancel</Button><Button type="submit" isLoading={isSubmitting}>Save Feedback</Button></div></form>};
