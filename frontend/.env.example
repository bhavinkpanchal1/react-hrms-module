# Django REST Framework API root. Do not add a trailing slash.
VITE_API_BASE_URL=http://localhost:8000/api

# The live Django API is used by default. Set true only for offline UI work.
VITE_USE_MOCK_API=false
